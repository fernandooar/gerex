RewriteEngine On
RewriteBase /

# Ignora arquivos e pastas reais
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tudo vai para index.php?rota=…
RewriteRule ^(.+)$ index.php?rota=$1 [QSA,L]
